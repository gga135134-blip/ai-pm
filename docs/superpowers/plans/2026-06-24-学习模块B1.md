# 建造师学习模块 B1（核心学习循环）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 ai-pm 里加一个「学习」模块，把 Phase A 四科 JSON 导入，做成每日计划 + 考点自评 + 母题/密卷练习 + 1-3-7 复习调度的核心学习循环（手机优先）。

**Architecture:** 调度逻辑（1-3-7、配额分配、选题、倒计时自适应、漏任务顺延）写成 `study_engine.py` 里的**纯函数**，用普通 pytest 测；DB（7 张 study_ 表）、导入、路由、模板是薄 I/O 层，沿用 ai-pm 现有 aiosqlite + APIRouter + Jinja2 模式，手动验。

**Tech Stack:** Python / FastAPI / aiosqlite / Jinja2 / Tailwind（ai-pm 现有）；新增 pytest（开发依赖）。

## Global Constraints

- 仓库 `D:\GAGA-5-25\ai-pm`，当前 `main` 分支——实现前先开特性分支 `feat/study-b1`。
- 不改动现有 projects/notes/tasks 模块；只新增 study_ 相关文件 + 在 SCHEMA/main.py/base.html 追加。
- DB 用 `from app.database import get_db`（aiosqlite，Row factory）；表用 `CREATE TABLE IF NOT EXISTS`，TEXT 主键，`created_at DATETIME DEFAULT CURRENT_TIMESTAMP`。
- 路由用 `APIRouter()`，handler `async def`，`db = await get_db()` … `finally: await db.close()`，返回 `request.app.state.templates.TemplateResponse(request, "x.html", {...})`。
- 1-3-7 间隔：`INTERVALS = [1, 3, 7, 15, 30]`（天），stage 为其下标 0..4。
- 四科配比：实务0.32 / 管理0.32 / 法规0.20 / 经济0.16。每日新学目标 `daily_new_target=20`。休息日 `rest_weekday=6`（周日，Python `date.weekday()` 周一=0…周日=6）。考试日 2026-09-12，冲刺起点 2026-08-15。
- 提交信息结尾加：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`（用 `git -c user.name="Claude" -c user.email="noreply@anthropic.com"`）。
- 页面手机优先：单列、底部大按钮操作条，复用 base.html 现有 Tailwind 响应式（`.nav-desktop`/`#mob-menu` 那套 media query）。

---

### Task 1: 特性分支 + pytest 基建 + 1-3-7 调度纯函数

**Files:**
- Create: `tests/__init__.py`（空）
- Create: `tests/test_study_engine.py`
- Create: `app/services/study_engine.py`
- Modify: `requirements.txt`（追加 `pytest`）

**Interfaces:**
- Produces: `INTERVALS = [1,3,7,15,30]`；`next_review(current_stage: int, passed: bool, today: datetime.date) -> tuple[int, datetime.date]`（current_stage=-1 表示尚未进复习队列；返回新 stage 和下次 due 日期）。

- [ ] **Step 1: 开特性分支**

Run:
```bash
cd /d/GAGA-5-25/ai-pm && git checkout -b feat/study-b1
```

- [ ] **Step 2: 追加 pytest 依赖**

在 `requirements.txt` 末尾追加一行：
```
pytest
```
然后 `pip install pytest -q`。

- [ ] **Step 3: 写失败测试** `tests/test_study_engine.py`

```python
import datetime
from app.services.study_engine import next_review, INTERVALS

T = datetime.date(2026, 6, 24)

def test_intervals_value():
    assert INTERVALS == [1, 3, 7, 15, 30]

def test_first_pass_enters_stage0_due_plus1():
    stage, due = next_review(-1, True, T)
    assert stage == 0
    assert due == T + datetime.timedelta(days=1)

def test_consecutive_pass_advances():
    stage, due = next_review(0, True, T)
    assert stage == 1 and due == T + datetime.timedelta(days=3)
    stage, due = next_review(1, True, T)
    assert stage == 2 and due == T + datetime.timedelta(days=7)

def test_pass_caps_at_last_stage():
    stage, due = next_review(4, True, T)
    assert stage == 4 and due == T + datetime.timedelta(days=30)

def test_fail_resets_to_stage0_due_tomorrow():
    stage, due = next_review(3, False, T)
    assert stage == 0 and due == T + datetime.timedelta(days=1)
```

- [ ] **Step 4: 跑测试确认失败**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_engine.py -v`
Expected: FAIL（`ModuleNotFoundError: app.services.study_engine` 或函数未定义）

- [ ] **Step 5: 实现** `app/services/study_engine.py`

```python
import datetime

INTERVALS = [1, 3, 7, 15, 30]  # 天；stage 为下标 0..4


def next_review(current_stage, passed, today):
    """
    1-3-7 调度。current_stage=-1 表示尚未进复习队列。
    passed=True(自评会/答对): stage 进一档(封顶), due=今天+INTERVALS[新档]。
    passed=False(模糊/不会/答错): stage 重置为 0, due=今天+1(明天)。
    """
    if passed:
        new_stage = min(current_stage + 1, len(INTERVALS) - 1)
    else:
        new_stage = 0
    due = today + datetime.timedelta(days=INTERVALS[new_stage])
    return new_stage, due
```

- [ ] **Step 6: 跑测试确认通过**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_engine.py -v`
Expected: PASS（5 passed）

- [ ] **Step 7: 提交**

```bash
git -c user.name="Claude" -c user.email="noreply@anthropic.com" add requirements.txt tests/ app/services/study_engine.py
git -c user.name="Claude" -c user.email="noreply@anthropic.com" commit -m "feat(study): pytest基建 + 1-3-7复习调度纯函数

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 每日新学配额分配（纯函数）

**Files:**
- Modify: `app/services/study_engine.py`
- Modify: `tests/test_study_engine.py`

**Interfaces:**
- Consumes: 无（纯函数）。
- Produces: `allocate_new_quota(due_count: int, today: datetime.date, sprint_date: datetime.date, weights: dict[str,float], daily_new_target: int = 20) -> dict[str,int]`。规则：到了冲刺日(today>=sprint_date)新学名额=0；否则有效新学 = max(0, daily_new_target - due_count)（复习多则新学少）；按 weights 分配到各科，四舍五入后把余数补给占比最大的科目，使各科之和=有效新学。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_study_engine.py`）

```python
from app.services.study_engine import allocate_new_quota

W = {"建筑实务": 0.32, "管理": 0.32, "法规": 0.20, "经济": 0.16}
SPRINT = datetime.date(2026, 8, 15)

def test_quota_sums_to_effective_new():
    q = allocate_new_quota(due_count=0, today=datetime.date(2026,6,24),
                           sprint_date=SPRINT, weights=W, daily_new_target=20)
    assert sum(q.values()) == 20
    assert q["建筑实务"] >= q["法规"] >= q["经济"]

def test_quota_shrinks_with_review_load():
    q = allocate_new_quota(due_count=15, today=datetime.date(2026,6,24),
                           sprint_date=SPRINT, weights=W, daily_new_target=20)
    assert sum(q.values()) == 5

def test_quota_zero_when_review_exceeds_target():
    q = allocate_new_quota(due_count=25, today=datetime.date(2026,6,24),
                           sprint_date=SPRINT, weights=W, daily_new_target=20)
    assert sum(q.values()) == 0

def test_quota_zero_after_sprint():
    q = allocate_new_quota(due_count=0, today=datetime.date(2026,8,20),
                           sprint_date=SPRINT, weights=W, daily_new_target=20)
    assert sum(q.values()) == 0
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_engine.py -v`
Expected: FAIL（`allocate_new_quota` 未定义）

- [ ] **Step 3: 实现**（追加到 `app/services/study_engine.py`）

```python
def allocate_new_quota(due_count, today, sprint_date, weights, daily_new_target=20):
    """按四科配比分配今日新学名额；复习多则缩、过冲刺日则停。"""
    if today >= sprint_date:
        effective = 0
    else:
        effective = max(0, daily_new_target - due_count)
    quota = {s: int(effective * w) for s, w in weights.items()}
    # 余数补给占比最大的科目，保证总和=effective
    remainder = effective - sum(quota.values())
    if remainder > 0:
        top = max(weights, key=weights.get)
        quota[top] += remainder
    return quota
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_engine.py -v`
Expected: PASS（9 passed）

- [ ] **Step 5: 提交**

```bash
git -c user.name="Claude" -c user.email="noreply@anthropic.com" add app/services/study_engine.py tests/test_study_engine.py
git -c user.name="Claude" -c user.email="noreply@anthropic.com" commit -m "feat(study): 每日新学配额分配(配比/复习缩/冲刺停)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: 今日计划组装（纯函数）

**Files:**
- Modify: `app/services/study_engine.py`
- Modify: `tests/test_study_engine.py`

**Interfaces:**
- Consumes: `allocate_new_quota`。
- Produces: `build_plan_items(due_reviews, unlearned_by_subject, today, sprint_date, weights, daily_new_target=20) -> list[dict]`。
  - `due_reviews`: list[dict]，每项 `{"item_type","item_id","subject"}`（已到期复习项，全部纳入，kind="review"）。
  - `unlearned_by_subject`: dict[subject, list[point_id]]，已按 importance 降序排好的未学考点 id。
  - 返回 list[dict]，每项 `{"item_type","item_id","subject","kind"}`，kind ∈ {"review","new"}。先放全部 review，再按各科 quota 从 unlearned 头部取 new。

- [ ] **Step 1: 写失败测试**（追加）

```python
from app.services.study_engine import build_plan_items

def test_build_plan_review_first_then_new():
    due = [{"item_type":"point","item_id":"ZS-经济-0001","subject":"经济"}]
    unlearned = {"建筑实务":["a1","a2","a3"], "管理":["b1"], "法规":[], "经济":["c1"]}
    items = build_plan_items(due, unlearned, datetime.date(2026,6,24),
                             SPRINT, W, daily_new_target=4)
    # 复习项排最前
    assert items[0] == {"item_type":"point","item_id":"ZS-经济-0001","subject":"经济","kind":"review"}
    # 有效新学=4-1=3, 配比下实务应分到最多
    new_items = [it for it in items if it["kind"] == "new"]
    assert len(new_items) == 3
    assert any(it["item_id"] == "a1" for it in new_items)  # 实务取头部

def test_build_plan_caps_new_at_available_unlearned():
    due = []
    unlearned = {"建筑实务":["a1"], "管理":[], "法规":[], "经济":[]}
    items = build_plan_items(due, unlearned, datetime.date(2026,6,24),
                             SPRINT, W, daily_new_target=20)
    new_items = [it for it in items if it["kind"]=="new"]
    assert len(new_items) == 1  # 只有 a1 可学，不超发
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_engine.py -v`
Expected: FAIL（`build_plan_items` 未定义）

- [ ] **Step 3: 实现**（追加到 `study_engine.py`）

```python
def build_plan_items(due_reviews, unlearned_by_subject, today, sprint_date,
                     weights, daily_new_target=20):
    """组装今日计划：先全部到期复习，再按各科配额取未学考点作为新学。"""
    items = [{"item_type": r["item_type"], "item_id": r["item_id"],
              "subject": r["subject"], "kind": "review"} for r in due_reviews]
    quota = allocate_new_quota(len(due_reviews), today, sprint_date,
                               weights, daily_new_target)
    for subject, n in quota.items():
        ids = unlearned_by_subject.get(subject, [])
        for pid in ids[:n]:
            items.append({"item_type": "point", "item_id": pid,
                          "subject": subject, "kind": "new"})
    return items
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_engine.py -v`
Expected: PASS（11 passed）

- [ ] **Step 5: 提交**

```bash
git -c user.name="Claude" -c user.email="noreply@anthropic.com" add app/services/study_engine.py tests/test_study_engine.py
git -c user.name="Claude" -c user.email="noreply@anthropic.com" commit -m "feat(study): 今日计划组装(复习优先+按配额取新学)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 7 张 study_ 表 + 配置种子（SCHEMA 校验测试）

**Files:**
- Modify: `app/database.py`（在 `SCHEMA` 字符串末尾、`"""` 之前追加 7 张表；在 `init_db()` 末尾种入 study_settings 默认行）
- Create: `tests/test_study_schema.py`

**Interfaces:**
- Produces: 表 `study_points, study_archetypes, study_questions, study_review, study_records, study_plan, study_settings`（字段见下）。

- [ ] **Step 1: 写失败测试** `tests/test_study_schema.py`（用 stdlib sqlite3 同步校验 SCHEMA 合法且建出 7 张表，无需 async）

```python
import sqlite3
from app.database import SCHEMA

EXPECTED = {"study_points","study_archetypes","study_questions",
            "study_review","study_records","study_plan","study_settings"}

def test_schema_creates_study_tables():
    con = sqlite3.connect(":memory:")
    con.executescript(SCHEMA)
    rows = con.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()
    names = {r[0] for r in rows}
    assert EXPECTED <= names
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_schema.py -v`
Expected: FAIL（缺这些表）

- [ ] **Step 3: 在 `app/database.py` 的 SCHEMA 末尾（结尾 `"""` 前）追加**

```sql
CREATE TABLE IF NOT EXISTS study_points (
    id TEXT PRIMARY KEY,
    subject TEXT NOT NULL,
    chapter TEXT DEFAULT '',
    title TEXT DEFAULT '',
    content TEXT DEFAULT '',
    type TEXT DEFAULT '[]',
    importance INTEGER DEFAULT 3,
    memory_method TEXT DEFAULT '',
    related TEXT DEFAULT '[]',
    source_refs TEXT DEFAULT '[]'
);

CREATE TABLE IF NOT EXISTS study_archetypes (
    id TEXT PRIMARY KEY,
    subject TEXT NOT NULL,
    stem TEXT DEFAULT '',
    solution_logic TEXT DEFAULT '',
    knowledge_points TEXT DEFAULT '[]',
    variants TEXT DEFAULT '[]'
);

CREATE TABLE IF NOT EXISTS study_questions (
    id TEXT PRIMARY KEY,
    subject TEXT NOT NULL,
    source TEXT DEFAULT '',
    qtype TEXT DEFAULT '单选',
    stem TEXT DEFAULT '',
    options TEXT DEFAULT '[]',
    answer TEXT DEFAULT '',
    maps_to TEXT DEFAULT '[]'
);

CREATE TABLE IF NOT EXISTS study_review (
    id TEXT PRIMARY KEY,
    item_type TEXT NOT NULL,
    item_id TEXT NOT NULL,
    subject TEXT DEFAULT '',
    stage INTEGER DEFAULT 0,
    due_date DATE,
    last_result TEXT DEFAULT '',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS study_records (
    id TEXT PRIMARY KEY,
    item_type TEXT NOT NULL,
    item_id TEXT NOT NULL,
    subject TEXT DEFAULT '',
    action TEXT DEFAULT '',
    plan_date DATE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS study_plan (
    id TEXT PRIMARY KEY,
    plan_date DATE NOT NULL,
    items TEXT DEFAULT '[]',
    status TEXT DEFAULT 'active',
    generated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS study_settings (
    id INTEGER PRIMARY KEY CHECK (id = 1),
    daily_minutes INTEGER DEFAULT 150,
    rest_weekday INTEGER DEFAULT 6,
    subject_weights TEXT DEFAULT '{"建筑实务":0.32,"管理":0.32,"法规":0.20,"经济":0.16}',
    exam_date DATE DEFAULT '2026-09-12',
    sprint_date DATE DEFAULT '2026-08-15',
    daily_new_target INTEGER DEFAULT 20
);
```

- [ ] **Step 4: 在 `init_db()` 的 `await db.commit()` 之前追加 study_settings 种子（幂等）**

```python
        await db.execute(
            "INSERT OR IGNORE INTO study_settings (id) VALUES (1)"
        )
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_schema.py -v`
Expected: PASS

- [ ] **Step 6: 手动验启动建表**

Run: `cd /d/GAGA-5-25/ai-pm && python -c "import asyncio; from app.database import init_db; asyncio.run(init_db()); print('init ok')"`
Expected: 打印 `init ok`，无异常。

- [ ] **Step 7: 提交**

```bash
git -c user.name="Claude" -c user.email="noreply@anthropic.com" add app/database.py tests/test_study_schema.py
git -c user.name="Claude" -c user.email="noreply@anthropic.com" commit -m "feat(study): 7张study_表 + 配置种子

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 导入脚本（纯转换测试 + 异步入库 + 拷贝 JSON）

**Files:**
- Create: `app/services/study_import.py`
- Create: `tests/test_study_import.py`
- Modify: `.gitignore`（加 `!data/study_source/` 例外）
- 数据：把 `D:\GAGA-5-25\建造师学习工具\data\output\*.json` 拷到 `D:\GAGA-5-25\ai-pm\data\study_source\`

**Interfaces:**
- Produces:
  - `point_rows(knowledge_json: list, subject: str) -> list[dict]`（纯转换；每行键与 study_points 列对齐，JSON 字段 `type/related/source_refs` 用 `json.dumps`）。
  - `archetype_rows(json, subject)`、`question_rows(json, subject)`（同上，对齐各表列）。
  - `async def import_all(db, source_dir)`（读四科 *_knowledge/_archetypes/_2026预测密卷.json，清空 study_points/archetypes/questions 后批量插入）。

- [ ] **Step 1: 写失败测试** `tests/test_study_import.py`

```python
import json
from app.services.study_import import point_rows, question_rows

def test_point_rows_maps_and_serializes():
    kj = [{"id":"ZS-经济-0001","subject":"经济","chapter":"一","title":"利息",
           "content":"...","type":["数字"],"importance":4,
           "memory_method":"数字集中归类","related":["ZS-经济-0002"],
           "source_refs":["速记 p1"]}]
    rows = point_rows(kj, "经济")
    assert len(rows) == 1
    r = rows[0]
    assert r["id"] == "ZS-经济-0001" and r["importance"] == 4
    assert json.loads(r["type"]) == ["数字"]          # JSON 字段已序列化为字符串
    assert json.loads(r["related"]) == ["ZS-经济-0002"]

def test_question_rows_maps_qtype_options():
    qj = [{"id":"Q-2026预测-经济-1","subject":"经济","source":"2026预测密卷",
           "type":"单选","stem":"...","options":["A","B"],"answer":"B","maps_to":[]}]
    rows = question_rows(qj, "经济")
    r = rows[0]
    assert r["qtype"] == "单选" and r["answer"] == "B"
    assert json.loads(r["options"]) == ["A","B"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_import.py -v`
Expected: FAIL（模块/函数未定义）

- [ ] **Step 3: 实现** `app/services/study_import.py`

```python
import json, os

SUBJECTS = ["建筑实务", "管理", "法规", "经济"]


def point_rows(knowledge_json, subject):
    out = []
    for k in knowledge_json:
        out.append({
            "id": k["id"], "subject": k.get("subject", subject),
            "chapter": k.get("chapter", ""), "title": k.get("title", ""),
            "content": k.get("content", ""),
            "type": json.dumps(k.get("type", []), ensure_ascii=False),
            "importance": k.get("importance", 3),
            "memory_method": k.get("memory_method", ""),
            "related": json.dumps(k.get("related", []), ensure_ascii=False),
            "source_refs": json.dumps(k.get("source_refs", []), ensure_ascii=False),
        })
    return out


def archetype_rows(arch_json, subject):
    out = []
    for a in arch_json:
        out.append({
            "id": a["id"], "subject": a.get("subject", subject),
            "stem": a.get("stem", ""), "solution_logic": a.get("solution_logic", ""),
            "knowledge_points": json.dumps(a.get("knowledge_points", []), ensure_ascii=False),
            "variants": json.dumps(a.get("variants", []), ensure_ascii=False),
        })
    return out


def question_rows(q_json, subject):
    out = []
    for q in q_json:
        out.append({
            "id": q["id"], "subject": q.get("subject", subject),
            "source": q.get("source", ""), "qtype": q.get("type", "单选"),
            "stem": q.get("stem", ""),
            "options": json.dumps(q.get("options", []), ensure_ascii=False),
            "answer": q.get("answer", ""),
            "maps_to": json.dumps(q.get("maps_to", []), ensure_ascii=False),
        })
    return out


def _load(path):
    if not os.path.exists(path):
        return []
    with open(path, encoding="utf-8") as f:
        return json.load(f)


async def _bulk(db, table, rows):
    if not rows:
        return
    cols = list(rows[0].keys())
    placeholders = ",".join("?" for _ in cols)
    sql = f"INSERT OR REPLACE INTO {table} ({','.join(cols)}) VALUES ({placeholders})"
    await db.executemany(sql, [[r[c] for c in cols] for r in rows])


async def import_all(db, source_dir):
    """读 source_dir 下四科 JSON，覆盖式导入 study_points/archetypes/questions。"""
    for t in ("study_points", "study_archetypes", "study_questions"):
        await db.execute(f"DELETE FROM {t}")
    for subj in SUBJECTS:
        await _bulk(db, "study_points",
                    point_rows(_load(os.path.join(source_dir, f"{subj}_knowledge.json")), subj))
        await _bulk(db, "study_archetypes",
                    archetype_rows(_load(os.path.join(source_dir, f"{subj}_archetypes.json")), subj))
        await _bulk(db, "study_questions",
                    question_rows(_load(os.path.join(source_dir, f"{subj}_2026预测密卷.json")), subj))
    await db.commit()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/test_study_import.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 拷贝 JSON + .gitignore 例外**

```bash
mkdir -p /d/GAGA-5-25/ai-pm/data/study_source
cp /d/GAGA-5-25/建造师学习工具/data/output/*.json /d/GAGA-5-25/ai-pm/data/study_source/
```
在 `.gitignore` 末尾追加（若 `data/` 被忽略，加例外让 study_source 入库）：
```
!data/study_source/
!data/study_source/*.json
```

- [ ] **Step 6: 手动验导入条数**

Run:
```bash
cd /d/GAGA-5-25/ai-pm && python -c "
import asyncio
from app.database import get_db, init_db
from app.services.study_import import import_all
async def m():
    await init_db()
    db=await get_db()
    await import_all(db,'data/study_source')
    for t in ('study_points','study_archetypes','study_questions'):
        c=await db.execute(f'SELECT COUNT(*) c FROM {t}'); print(t,(await c.fetchone())['c'])
    await db.close()
asyncio.run(m())"
```
Expected: study_points≈3037、study_archetypes≈122、study_questions≈125（四科密卷合计；与 Phase A 一致量级）。

- [ ] **Step 7: 提交**

```bash
git -c user.name="Claude" -c user.email="noreply@anthropic.com" add app/services/study_import.py tests/test_study_import.py .gitignore data/study_source/
git -c user.name="Claude" -c user.email="noreply@anthropic.com" commit -m "feat(study): 四科JSON导入(纯转换+异步入库)+数据入库

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: study 路由 + 异步引擎 DB 层 + 挂载

**Files:**
- Create: `app/api/study.py`
- Modify: `app/services/study_engine.py`（追加异步 DB 层）
- Modify: `app/main.py`（import study + `app.include_router(study.router)`）

**Interfaces:**
- Consumes: `next_review, allocate_new_quota, build_plan_items`（纯函数）；`get_db`。
- Produces:
  - `async def get_or_build_today_plan(db, today)`：当天已有 study_plan 则返回；否则查到期复习 + 各科未学考点(按 importance desc、排除已在 study_review 的)，调用 `build_plan_items`，存入 study_plan 并返回。
  - `async def record_action(db, item_type, item_id, action)`：写 study_records；据 action(会/对→passed=True；模糊/不会/错→False)用 next_review 更新/插入 study_review。
  - 路由：`GET /study`、`GET /study/point/{id}`、`GET /study/archetype/{id}`、`GET /study/exam/{subject}`、`GET /study/review`、`POST /study/record`。

- [ ] **Step 1: 在 `study_engine.py` 追加异步 DB 层**

```python
import datetime as _dt, json as _json, uuid as _uuid

PASS_ACTIONS = {"会", "对"}

async def _settings(db):
    cur = await db.execute("SELECT * FROM study_settings WHERE id=1")
    return await cur.fetchone()

async def get_or_build_today_plan(db, today=None):
    today = today or _dt.date.today()
    iso = today.isoformat()
    cur = await db.execute("SELECT * FROM study_plan WHERE plan_date=?", (iso,))
    row = await cur.fetchone()
    if row:
        return _json.loads(row["items"])
    s = await _settings(db)
    weights = _json.loads(s["subject_weights"])
    sprint = _dt.date.fromisoformat(s["sprint_date"])
    # 到期复习
    cur = await db.execute(
        "SELECT item_type,item_id,subject FROM study_review WHERE due_date<=?", (iso,))
    due = [dict(r) for r in await cur.fetchall()]
    # 各科未学考点(不在 study_review 的 point)，按 importance desc
    unlearned = {}
    for subj in weights:
        cur = await db.execute(
            """SELECT id FROM study_points WHERE subject=? AND id NOT IN
               (SELECT item_id FROM study_review WHERE item_type='point')
               ORDER BY importance DESC""", (subj,))
        unlearned[subj] = [r["id"] for r in await cur.fetchall()]
    items = build_plan_items(due, unlearned, today, sprint, weights,
                             s["daily_new_target"])
    await db.execute(
        "INSERT INTO study_plan (id,plan_date,items) VALUES (?,?,?)",
        (str(_uuid.uuid4()), iso, _json.dumps(items, ensure_ascii=False)))
    await db.commit()
    return items

async def record_action(db, item_type, item_id, action, subject=""):
    today = _dt.date.today()
    await db.execute(
        "INSERT INTO study_records (id,item_type,item_id,subject,action,plan_date) "
        "VALUES (?,?,?,?,?,?)",
        (str(_uuid.uuid4()), item_type, item_id, subject, action, today.isoformat()))
    passed = action in PASS_ACTIONS
    cur = await db.execute(
        "SELECT * FROM study_review WHERE item_type=? AND item_id=?", (item_type, item_id))
    row = await cur.fetchone()
    cur_stage = row["stage"] if row else -1
    stage, due = next_review(cur_stage, passed, today)
    if row:
        await db.execute(
            "UPDATE study_review SET stage=?,due_date=?,last_result=?,"
            "updated_at=CURRENT_TIMESTAMP WHERE id=?",
            (stage, due.isoformat(), action, row["id"]))
    else:
        await db.execute(
            "INSERT INTO study_review (id,item_type,item_id,subject,stage,due_date,last_result) "
            "VALUES (?,?,?,?,?,?,?)",
            (str(_uuid.uuid4()), item_type, item_id, subject, stage, due.isoformat(), action))
    await db.commit()
```

- [ ] **Step 2: 实现** `app/api/study.py`

```python
import datetime, json
from fastapi import APIRouter, Request, Form
from fastapi.responses import HTMLResponse, RedirectResponse
from app.database import get_db
from app.services.study_engine import get_or_build_today_plan, record_action

router = APIRouter()


def _tpl(request, name, ctx):
    ctx["request"] = request
    return request.app.state.templates.TemplateResponse(request, name, ctx)


@router.get("/study", response_class=HTMLResponse)
async def study_home(request: Request):
    db = await get_db()
    try:
        items = await get_or_build_today_plan(db)
        ids = [i["item_id"] for i in items]
        titles = {}
        if ids:
            q = ",".join("?" for _ in ids)
            cur = await db.execute(f"SELECT id,title FROM study_points WHERE id IN ({q})", ids)
            titles = {r["id"]: r["title"] for r in await cur.fetchall()}
        # 倒计时 + 掌握度
        cur = await db.execute("SELECT exam_date FROM study_settings WHERE id=1")
        exam = datetime.date.fromisoformat((await cur.fetchone())["exam_date"])
        days_left = (exam - datetime.date.today()).days
        cur = await db.execute("SELECT subject,COUNT(*) c FROM study_review WHERE stage>=3 GROUP BY subject")
        mastered = {r["subject"]: r["c"] for r in await cur.fetchall()}
        reviews = [i for i in items if i["kind"] == "review"]
        news = [i for i in items if i["kind"] == "new"]
    finally:
        await db.close()
    return _tpl(request, "study_today.html",
                {"reviews": reviews, "news": news, "titles": titles,
                 "days_left": days_left, "mastered": mastered})


@router.get("/study/point/{pid}", response_class=HTMLResponse)
async def study_point(request: Request, pid: str):
    db = await get_db()
    try:
        cur = await db.execute("SELECT * FROM study_points WHERE id=?", (pid,))
        row = await cur.fetchone()
        point = dict(row) if row else None
        if point:
            point["type"] = json.loads(point["type"])
            point["related"] = json.loads(point["related"])
    finally:
        await db.close()
    return _tpl(request, "study_point.html", {"point": point})


@router.get("/study/archetype/{aid}", response_class=HTMLResponse)
async def study_archetype(request: Request, aid: str):
    db = await get_db()
    try:
        cur = await db.execute("SELECT * FROM study_archetypes WHERE id=?", (aid,))
        row = await cur.fetchone()
        arch = dict(row) if row else None
        if arch:
            arch["variants"] = json.loads(arch["variants"])
            arch["knowledge_points"] = json.loads(arch["knowledge_points"])
    finally:
        await db.close()
    return _tpl(request, "study_archetype.html", {"arch": arch})


@router.get("/study/exam/{subject}", response_class=HTMLResponse)
async def study_exam(request: Request, subject: str):
    db = await get_db()
    try:
        cur = await db.execute(
            "SELECT * FROM study_questions WHERE subject=? AND source='2026预测密卷'", (subject,))
        qs = []
        for r in await cur.fetchall():
            d = dict(r); d["options"] = json.loads(d["options"]); qs.append(d)
    finally:
        await db.close()
    return _tpl(request, "study_exam.html", {"subject": subject, "questions": qs})


@router.get("/study/review", response_class=HTMLResponse)
async def study_review_page(request: Request):
    db = await get_db()
    try:
        today = datetime.date.today().isoformat()
        cur = await db.execute(
            "SELECT * FROM study_review WHERE due_date<=? ORDER BY due_date", (today,))
        due = [dict(r) for r in await cur.fetchall()]
    finally:
        await db.close()
    return _tpl(request, "study_review.html", {"due": due})


@router.post("/study/record")
async def study_record(request: Request, item_type: str = Form(...),
                       item_id: str = Form(...), action: str = Form(...),
                       subject: str = Form(""), next_url: str = Form("/study")):
    db = await get_db()
    try:
        await record_action(db, item_type, item_id, action, subject)
    finally:
        await db.close()
    return RedirectResponse(next_url, status_code=303)
```

- [ ] **Step 3: 挂载路由** — 在 `app/main.py` 的 import 行加 `study`，并加 `app.include_router(study.router)`

```python
from app.api import dashboard, projects, tasks, settings, agents, notes, chat, auth, finance, study
```
```python
app.include_router(study.router)
```

- [ ] **Step 4: 全量测试仍绿**

Run: `cd /d/GAGA-5-25/ai-pm && python -m pytest tests/ -q`
Expected: 14 passed（11 engine + 1 schema + 2 import；纯函数测试不受影响）

- [ ] **Step 5: 手动验路由可用（需先有 Task5 的数据）**

Run: `cd /d/GAGA-5-25/ai-pm && python -c "
import asyncio
from app.database import get_db
from app.services.study_engine import get_or_build_today_plan
async def m():
    db=await get_db(); items=await get_or_build_today_plan(db)
    print('今日计划项数:',len(items),'复习:',sum(1 for i in items if i[\"kind\"]==\"review\"),'新学:',sum(1 for i in items if i[\"kind\"]==\"new\")); await db.close()
asyncio.run(m())"`
Expected: 打印今日计划项数（首日应是纯新学约 20 项）。

- [ ] **Step 6: 提交**

```bash
git -c user.name="Claude" -c user.email="noreply@anthropic.com" add app/api/study.py app/services/study_engine.py app/main.py
git -c user.name="Claude" -c user.email="noreply@anthropic.com" commit -m "feat(study): 学习路由 + 计划/记录异步引擎 + 挂载

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: 手机优先模板 + 导航入口

**Files:**
- Create: `app/templates/study_today.html`、`study_point.html`、`study_archetype.html`、`study_exam.html`、`study_review.html`
- Modify: `app/templates/base.html`（桌面导航 + 移动菜单各加「🎓 学习」入口）

**Interfaces:**
- Consumes: Task6 路由传入的 ctx 变量（reviews/news/titles/days_left/mastered；point；arch；subject/questions；due）。

- [ ] **Step 1: base.html 加导航**（桌面区，在「知识库」`/notes` 那条 `<a>` 之后加一行）

```html
<a href="/study" class="text-gray-600 hover:text-gray-900 pb-0.5 {% if path.startswith('/study') %}border-b-2 border-purple-500 font-medium{% endif %}">🎓 学习</a>
```
移动菜单区（`#mob-menu` 内，知识库那条之后加一行）：
```html
<a href="/study" class="flex items-center gap-3 px-3 py-2.5 rounded-lg {% if path.startswith('/study') %}bg-purple-50 text-purple-700 font-medium{% else %}text-gray-700 hover:bg-gray-50{% endif %}">🎓 学习</a>
```

- [ ] **Step 2: `study_today.html`**（手机优先：单列卡片 + 顶部倒计时/进度）

```html
{% extends "base.html" %}
{% block title %}今日学习{% endblock %}
{% block content %}
<div class="max-w-2xl mx-auto px-3 py-4">
  <div class="bg-white rounded-xl shadow-sm p-4 mb-4">
    <div class="text-sm text-gray-500">距考试</div>
    <div class="text-3xl font-bold text-purple-600">{{ days_left }} 天</div>
    <div class="text-xs text-gray-400 mt-1">掌握(stage≥3)：
      {% for s,c in mastered.items() %}{{ s }} {{ c }}　{% endfor %}</div>
  </div>

  <h2 class="text-base font-semibold text-gray-700 mb-2">🔁 复习（{{ reviews|length }}）</h2>
  {% for it in reviews %}
  <a href="{% if it.item_type=='point' %}/study/point/{{ it.item_id }}{% else %}/study/review{% endif %}"
     class="block bg-white rounded-lg shadow-sm p-3 mb-2 active:bg-gray-50">
    <span class="text-xs text-purple-500">{{ it.subject }}</span>
    <div class="text-sm text-gray-800">{{ titles.get(it.item_id, it.item_id) }}</div>
  </a>
  {% else %}<p class="text-sm text-gray-400 mb-3">今日无到期复习</p>{% endfor %}

  <h2 class="text-base font-semibold text-gray-700 mb-2 mt-4">📖 新学（{{ news|length }}）</h2>
  {% for it in news %}
  <a href="/study/point/{{ it.item_id }}"
     class="block bg-white rounded-lg shadow-sm p-3 mb-2 active:bg-gray-50">
    <span class="text-xs text-blue-500">{{ it.subject }}</span>
    <div class="text-sm text-gray-800">{{ titles.get(it.item_id, it.item_id) }}</div>
  </a>
  {% else %}<p class="text-sm text-gray-400">今日新学已排完</p>{% endfor %}
</div>
{% endblock %}
```

- [ ] **Step 3: `study_point.html`**（正文 + 底部固定 会/模糊/不会）

```html
{% extends "base.html" %}
{% block title %}考点{% endblock %}
{% block content %}
{% if point %}
<div class="max-w-2xl mx-auto px-3 py-4 pb-24">
  <span class="text-xs text-purple-500">{{ point.subject }} · {{ point.chapter }}</span>
  <h1 class="text-lg font-bold text-gray-900 mt-1 mb-3">{{ point.title }}</h1>
  <div class="bg-white rounded-lg shadow-sm p-4 text-sm text-gray-800 leading-relaxed whitespace-pre-wrap">{{ point.content }}</div>
  {% if point.memory_method %}<div class="mt-3 text-sm text-amber-700 bg-amber-50 rounded-lg p-3">💡 记忆法：{{ point.memory_method }}</div>{% endif %}
</div>
<form method="post" action="/study/record"
      class="fixed bottom-0 inset-x-0 bg-white border-t flex gap-2 p-3 max-w-2xl mx-auto">
  <input type="hidden" name="item_type" value="point">
  <input type="hidden" name="item_id" value="{{ point.id }}">
  <input type="hidden" name="subject" value="{{ point.subject }}">
  <button name="action" value="会" class="flex-1 py-3 rounded-lg bg-green-500 text-white font-medium">会</button>
  <button name="action" value="模糊" class="flex-1 py-3 rounded-lg bg-amber-400 text-white font-medium">模糊</button>
  <button name="action" value="不会" class="flex-1 py-3 rounded-lg bg-red-400 text-white font-medium">不会</button>
</form>
{% else %}<p class="p-4 text-gray-500">考点不存在</p>{% endif %}
{% endblock %}
```

- [ ] **Step 4: `study_archetype.html`**（母题 + 变体 + 底部 会/不会）

```html
{% extends "base.html" %}
{% block title %}母题{% endblock %}
{% block content %}
{% if arch %}
<div class="max-w-2xl mx-auto px-3 py-4 pb-24">
  <span class="text-xs text-purple-500">{{ arch.subject }} 母题</span>
  <div class="bg-white rounded-lg shadow-sm p-4 mt-1 text-sm text-gray-900 whitespace-pre-wrap">{{ arch.stem }}</div>
  <div class="mt-3 text-sm text-gray-700 bg-gray-50 rounded-lg p-3 whitespace-pre-wrap">🧭 解题逻辑：{{ arch.solution_logic }}</div>
  <h3 class="text-sm font-semibold text-gray-600 mt-4 mb-2">变体练习</h3>
  {% for v in arch.variants %}
  <div class="bg-white rounded-lg shadow-sm p-3 mb-2 text-sm">
    <div class="text-gray-800 whitespace-pre-wrap">{{ v.stem }}</div>
    <details class="mt-2"><summary class="text-xs text-purple-500">看答案</summary>
      <div class="text-xs text-gray-600 mt-1">{{ v.answer }}　<span class="text-gray-400">{{ v.note }}</span></div></details>
  </div>
  {% endfor %}
</div>
<form method="post" action="/study/record"
      class="fixed bottom-0 inset-x-0 bg-white border-t flex gap-2 p-3 max-w-2xl mx-auto">
  <input type="hidden" name="item_type" value="archetype">
  <input type="hidden" name="item_id" value="{{ arch.id }}">
  <input type="hidden" name="subject" value="{{ arch.subject }}">
  <button name="action" value="会" class="flex-1 py-3 rounded-lg bg-green-500 text-white font-medium">会</button>
  <button name="action" value="不会" class="flex-1 py-3 rounded-lg bg-red-400 text-white font-medium">不会</button>
</form>
{% else %}<p class="p-4 text-gray-500">母题不存在</p>{% endif %}
{% endblock %}
```

- [ ] **Step 5: `study_exam.html`**（密卷：题目+选项+答案折叠，逐题记录对错）

```html
{% extends "base.html" %}
{% block title %}{{ subject }}密卷{% endblock %}
{% block content %}
<div class="max-w-2xl mx-auto px-3 py-4">
  <h1 class="text-lg font-bold text-gray-900 mb-3">{{ subject }} · 2026预测密卷</h1>
  {% for q in questions %}
  <div class="bg-white rounded-lg shadow-sm p-4 mb-3">
    <div class="text-xs text-gray-400">{{ loop.index }} / {{ q.qtype }}</div>
    <div class="text-sm text-gray-900 mt-1 whitespace-pre-wrap">{{ q.stem }}</div>
    <ul class="mt-2 text-sm text-gray-700">
      {% for opt in q.options %}<li class="py-0.5">{{ opt }}</li>{% endfor %}
    </ul>
    <details class="mt-2"><summary class="text-xs text-purple-500">看答案</summary>
      <div class="text-sm text-green-700 mt-1">答案：{{ q.answer }}</div></details>
    <form method="post" action="/study/record" class="flex gap-2 mt-2">
      <input type="hidden" name="item_type" value="question">
      <input type="hidden" name="item_id" value="{{ q.id }}">
      <input type="hidden" name="subject" value="{{ subject }}">
      <input type="hidden" name="next_url" value="/study/exam/{{ subject }}">
      <button name="action" value="对" class="px-4 py-1.5 rounded bg-green-500 text-white text-sm">做对</button>
      <button name="action" value="错" class="px-4 py-1.5 rounded bg-red-400 text-white text-sm">做错</button>
    </form>
  </div>
  {% else %}<p class="text-sm text-gray-400">该科暂无密卷题</p>{% endfor %}
</div>
{% endblock %}
```

- [ ] **Step 6: `study_review.html`**（到期复习项，复用考点跳转）

```html
{% extends "base.html" %}
{% block title %}复习{% endblock %}
{% block content %}
<div class="max-w-2xl mx-auto px-3 py-4">
  <h1 class="text-lg font-bold text-gray-900 mb-3">🔁 到期复习（{{ due|length }}）</h1>
  {% for it in due %}
  <a href="{% if it.item_type=='point' %}/study/point/{{ it.item_id }}{% else %}/study/exam/{{ it.subject }}{% endif %}"
     class="block bg-white rounded-lg shadow-sm p-3 mb-2 active:bg-gray-50">
    <span class="text-xs text-purple-500">{{ it.subject }} · {{ it.item_type }} · 第{{ it.stage }}档</span>
    <div class="text-sm text-gray-800">{{ it.item_id }}</div>
  </a>
  {% else %}<p class="text-sm text-gray-400">暂无到期复习 🎉</p>{% endfor %}
</div>
{% endblock %}
```

- [ ] **Step 7: 手动验（手机视口）**

启动：`cd /d/GAGA-5-25/ai-pm && python run.py`（或现有启动方式），浏览器开 `/study`，用开发者工具切手机视口（如 iPhone 宽度）。检查：今日学习页单列正常、点考点能看正文+底部三按钮、点「会」后跳转且不再出现在明日新学、母题/密卷页可操作。导航栏（桌面+手机菜单）出现「🎓 学习」高亮。

- [ ] **Step 8: 提交**

```bash
git -c user.name="Claude" -c user.email="noreply@anthropic.com" add app/templates/
git -c user.name="Claude" -c user.email="noreply@anthropic.com" commit -m "feat(study): 手机优先学习页面5个 + 导航入口

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 完成后

7 个任务完成后，B1 核心学习循环即可在 ai-pm 上使用（本机 + 部署到服务器：`git pull` 带来 study_source，服务器跑一次导入命令即可）。下一步：
- 部署到服务器并跑导入。
- 进入 B2（企业微信提醒 + 实务案例 AI 采分点评分 + 错题加权），单独 spec。
