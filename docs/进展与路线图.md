# ai-pm 进展与路线图

> 项目的人读版进展记录（提交历史之上的一层"为什么/定了什么/接下来做什么"）。
> 生产：http://159.75.200.213:8000（systemd 服务 `ai-pm.service`，**不是 pm2**）。
> 更新流程：`cd /www/wwwroot/ai-pm && git pull && systemctl restart ai-pm`（连不上 GitHub 用镜像 `git pull https://gh-proxy.com/https://github.com/gga135134-blip/ai-pm.git main`）。

---

## 2026-08-14 · 自媒体二期「飞轮」大推进

这一天把自媒体模块从"能记录"推进到"能沉淀 + 能复用 + 多人设"。按上线顺序：

### 1. 老文案炼化 + 打法库🅐（merge `e0fbf25`）
- 老文案批量导入（粘文本→按序号切→预览确认→建成"已发/反向"内容 `media_content`，`idea_source=legacy_text`）。
- 挖精华加第三桶「结构/打法」（**仅爆款**）→ 新建**打法库** `media_playbook`。
- `is_winner` 标爆款（批量勾选）；采纳时 `similar_to` 归并/新建。
- **本轮零下游注入**：打法库只"装着"，先不碰写稿/决策。

### 2. 自媒体多人设总览 + 工作区隔离（merge `4996ac9`）
- `/media` 变**人设总览页**（每人设一张卡：定位/阶段/内容数/账号 + ➕新建），点进各自独立工作区，**cookie 记住当前人设**。
- 一份代码、一套功能——更新任何功能所有人设自动生效，**永不需要每人设一套系统**。
- **共享 vs 独享**（定了）：
  - **打法库 = 全公司共享**（可迁移方法论，一池子）。
  - **原料库 = 独享，但留了 `scope` 接口**（现纯隔离，将来一键开"公司级共享"）。
  - 其余（人设特征/账号/选题/内容/受众/锚点/复盘）= 独享。
- 底层地基早就是 `persona_id` 隔离，这轮只补了"选当前人设"的 cookie 层 + 总览页。

### 3. 自媒体模块 UI 重做（merge `f29ebf9`）
- 统一壳组件 `_media_shell.html`（体系库卡片 + 步骤条 + 面包屑），各页接壳。

### 4. 打法库🅓 接写稿（merge `bea0504`）
- 写口播脚本时，AI 从共享打法库挑**最贴选题的一条**打法当骨架注入。
- **注意力纪律**（最高优先级）：整个打法库只在**独立的 `match_playbook` 调用**里出现一次；写稿 prompt **只注匹配到的那一条**，匹配不到就零注入。
- 透明可测：写完显示"本次用了《X》· 理由"，可【换一条】/【不用打法重写】（现成 A/B 对比）。

### 5. 老文案批量挖矿 · 两条流（merge `1a129b4`）
- **流A · 批量挖记忆点**（口头禅→人设 signature）：从**所有**老文案，批量多选。
- **流B · 批量挖精华**（素材→原料库 + 打法→打法库）：**只从爆款**。
- **纪律**：批量 = 前端**逐条**喂 AI（每条独立 prompt），带进度。
- 候选进**暂存表** `media_mine_candidate`（跨刷新不丢/不白花钱）→ 复核页**去重分组**（同句合并显"出现 N 次"）→ 复选**批量采纳**。
- 老文案列表**已挖标识** `记忆点✓`/`精华✓`，已挖默认跳过（可强制重挖）。

### 修复（用户实测发现，随手修）
- 批量导入老文案弹窗点了没反应 → 补 `#legacy-overlay` 遮罩 CSS。
- 发布时间保存无反馈 → 改 AJAX「✓ 已保存」。
- 重复内容删不掉 → 加内容删除（清 6 张子表防 FK 孤儿）。
- 标爆款表单跳裸 JSON 页 → 改重定向回老文案页。
- 挖精华超时空返回显示 SyntaxError → 改友好提示。

---

## 2026-08-23 · 打法库接决策 + 反向批量 + 复盘助手打磨

这一天把「打法库」真正接进决策引擎（🅒），补齐视频批量入库，并打磨复盘助手体验 + 若干真机反馈修复。按上线顺序：

### 1. 复盘助手三处 UI 打磨（commit `7c683b1` 及前序）
- **挪位置**：复盘助手对话框从复盘页**底部→顶部**（简介下、已发内容上），跟内容/发布步骤同区位（用户："像内容·发布那样的位置"）。
- **对话渲染 markdown**：`_media_assistant_box.html` 的 `bubble()` 从"纯文本平铺"改成**轻量 vanilla-JS markdown 渲染器**（标题/粗细/斜体/表格/有序无序列表/分割线/行内码/代码块/段落，表情直接透传）。**先全量转义 `&<>` 再渲染防 XSS**。
- **排版打磨**：段落 margin 6→11px + 行高 1.75；标题 h1/h2/h3 放大到 21/18/15.5px 与正文 13.5 明显拉开；`media_assistant.py` 的 `MEDIA_ASSISTANT_SYSTEM` 补排版指引（`##` 分段/列表/表格/加粗/适当表情 ✅⚠️📊，叮嘱别滥用）——**让 AI 输出自带结构，不只是前端能渲染**。
- **踩坑**：markdown 代码块哨兵用 `String.fromCharCode(0xE000)`（U+E000 私有区字符），Write 后 grep 显示成空串 `''` 但字节正确（`od -c` 是 `356 200 200`）——别被显示骗了去"修"它。

### 2. 复盘助手「清空/新话题」按钮（commit `17cdb88`）
- 对话框加「🧹 清空/新话题」按钮（二次确认 → POST `/media/assistant/clear` → **原地重载不跳页**）；`assistant_clear` 从 303 跳转改返 JSON 供 AJAX。复盘页 + 独立助手页共享同一个 `_media_assistant_box.html`，一处加两处生效。
- 只删聊天记录，**沉淀进库的打法/素材/爆款不受影响**，不可撤。
- **AI 记忆机制（跟用户讲清）**：助手每轮只把**最近 10 条**历史喂给 AI（滑动窗口），全部历史存 `media_assistant_message` 表；真正的"记忆"在**库表**（内容/打法等）不在聊天；**无向量/语义索引**，AI 需要数据时调工具**现查 SQL**（`WHERE persona_id ORDER BY LIMIT`）——这就是注意力纪律在代码里的样子。独立助手页与复盘页嵌入框**共用同一份历史**（同人设同表），是同一场对话。

### 3. 打法库🅒 接决策引擎（merge `2f4c9de`，subagent 双审 + opus 整分支审 Ready to merge）
把决策引擎里恒为 0 的 `playbook` 因子真正接上——**方式与受众/锚点因子完全同构**（AI 打标 → 纯打分器读标）。4 任务 TDD，366 测绿。
- **AI 打标**：扩展现有 `tag_topics`，让 AI 给每个选题挑**一条**最贴的打法写进 `media_topic.playbook_id`（新列）。`_build_asset_menu` 加【打法库】段，`TAG_SYSTEM` 加 `playbook_id` 字段，新 helper `_clean_one_id` 清洗（越界/瞎编 id 丢弃成空）。
- **status 加权**：`proven=1.0 / validating=0.6 / 没命中=0`，与引擎里锚点 `_ANCHOR_STATUS_W` 同一路子（`_PLAYBOOK_STATUS_W`）。
- **命中才计入归一化分母**（关键语义，与用户敲定）：`playbook` 只在 `value>0`（真匹配）时才进 `pos` 正项列表。**没匹配的选题分数与今天完全一致、不被稀释**；匹配到的才纯加分。理由：爆款少、多数选题无匹配打法，若永久进分母会让整池绝对分普遍下滑（排序不变但看着分低了）。playbook 定位是"加分项/辅助杠杆"不是主导因子。
- **阶段权重**：冷启动 1 / 涨粉 2 / 转化 2（对齐 material_ready，不盖过 fit/audience/anchor 那几个 3-4 分主力）。理由（用户）：当前爆款少、可复制打法本就不多，给小权重合适；库攒厚后 L2 复盘再抬（改 `WEIGHTS` 一行）。
- **对标来源留后**：用户提"对标/拆解别人的打法该给更高分"——但系统现在**产不出对标打法**（`media_playbook.source` 只有 `legacy_mine`），本轮不做，权重差异化等以后能产出对标打法了单独一轮。
- **调用侧**：`media.py` 决策打分路由（rank）查该人设 validating+proven 打法传进 `build_decision_context(playbooks=)`；topics 是 `SELECT *` 自动带出新列。
- **留下 3 个 Minor**（最终审判定全可延后，非阻塞）：①无"命中但 status 未知(如 dropped)"测试（note 仍说匹配但 value=0 不渲染，零影响）②`value>0` 条件在 pos/报告两处重复可抽局部变量 ③无"零可用打法"下 tag_topics 写路径测试。
- **踩坑（重要）**：浏览器冒烟时发现 Windows 上 `pkill -f uvicorn` **杀不掉**旧 server 进程（进程名匹配不到），旧端口一直占着跑**旧代码**，第一次冒烟读到旧结果差点误判功能没生效（手动复现才发现代码是对的）。最终冒烟：proven 打法把选题分 0.4→1.7，报告真出「＋匹配到打法《反常识开场》（proven）」。

### 4. 写脚本空响应自动重试（commit `cb40d6f`）
- 起因：真机 DeepSeek 偶发返空，`write_script` 报"AI 返回了空内容，请重试（模型偶尔抽风）"。
- 修：空内容（非 `[错误]`/`[费用保护]`）时**后台悄悄重试一次**再放弃，**两次费用都算钱、只重试一次不死循环**。挡掉大部分偶发空返回，不用用户手动点。3 个新测（重试成功/两次都空报错/明确错误不重试）。

### 5. 老文案挖矿进度放大（commit `714b2a1`）
- 起因：用户反馈批量挖矿的进度文字太小看不清。
- 修：`#mine-progress` 12.5→15.5px 加粗；运行/完成态改成**带边框的高亮块**（⏳ 挖矿中… / ✅ 完成 X/Y，16px）；「去复核采纳」链接改按钮样式放大。

### 6. 视频批量粘链接反向入库（merge `34fb520`，inline TDD 3 任务，380 测绿）
反向入库单条已通，加"一次贴多条链接排队后台跑"。
- **决策**：一次最多 **10 条**（用户选，防手滑烧 ASR 费）；**精确 url 跳过已入库**（按 `media_content.idea_reason` 精确字符串匹配，同视频不同跟踪参数认不出算不同——第一版够用不做视频指纹）；**逐条串行**跑（避免 ASR 限流/被抖音封）；进度**只在反向 tab**（不铺全局条）。
- **新建 `app/services/media_reverse_batch.py`**：`parse_urls`（按行抠链接/贴内去重/截 10）+ 后台跑器 `_rev_jobs`（仿 `media_batch`，每人设一活跃任务、逐条调 `reverse_ingest`、**单条失败不中断**）。
- 端点 `POST /media/reverse/batch`（去重 + 起任务）+ `GET /media/reverse/batch-status`（轮询）。前端 `#rev-overlay` 加 textarea + 批量入库按钮 + 逐条 ✅/❌ 结果 + "跳过 N 条已入库"提示。零迁移。
- **踩坑**：`first_url()` 找不到链接会**原样返回 `text.strip()`**（不是空串），故 `parse_urls` 必须只保留 `http` 开头的结果，否则非链接行会被当 url。
- **诚实边界**：进程内内存任务，离页不断、**服务器重启中断**（已入库保留、没跑的丢），与 media_batch 一致。
- **本地限制**：本地没配豆包 ASR 凭证，controller 冒烟只验到 UI + 端点接线（返 200 JSON 无 500、status 结构对）；完整入库路径靠单测覆盖，真机（有豆包凭证）走完整流程。

### 重要澄清：打法库空时"打法 0"是正常的（不是 bug）
用户测写脚本/决策时看到"打法 0"疑惑。查明：那个计数来自 `list_playbooks(db)`，查的是**整张 `media_playbook` 表**（打法库跨人设**共享**、不分人设）。显示 0 = 库里一条打法都没有。要让它非 0（进而 🅒 决策打分 / 🅓 写稿真正用上打法），得先走**标爆款 → 挖精华 → 结构/打法桶 → 采纳进打法库**。功能都通了，只是"料"还没进库——正好对上用户"现在爆款少、可复制打法不多"。

---

## 关键设计决策（贯穿始终的"宪法"）

1. **AI 注意力纪律（项目核心）**：任何往 prompt 堆信息的设计先过这关。"档案柜不是会议桌"——库只把**当前用得上的那一条**拿到桌上，绝不整库喂干活 AI。整库只能出现在**隔离的匹配调用**或**给人看的 UI** 里。
2. **人拍板**：AI 只提候选，绝不自动写库；人 adopt 才入。诚实不编造（只从真实来源挖，evidence 引原文）。
3. **取材范围按方法论分**：打法/素材**只从真爆款**抽（不爆的是噪音）；口头禅**从所有**老文案抽（腔调不看爆没爆）。
4. **共享边界**：打法库共享（方法论可迁移）；素材/人设独享（是"这个人是谁"）；原料库留 `scope` 接口备将来公司级共享。
5. **能复用别新建**：老文案 + 老视频统一走"反向补录已发内容"；批量挖复用现有 `mine_from_transcript`/`mine_structure`；采纳复用现有落库逻辑。
6. **分层人拍板**（对话助手落地）：查=免确认无痕；改草稿（选题/续集/脚本/整理格式）=AI 直接做但**留痕可撤**；核心动作（标爆款/删除/采纳入库/跑复盘）=**待确认卡**，点确认才执行、也留痕；**删除确认后不可撤**——确认卡就是它的安全闸。
7. **助手动作日志是通用留痕/撤销底座**（`media_assistant_action`）：任何"AI 改写/写库"类新功能，复用 `log_action` + 给 `revert_action` 加一个 `action_type` 分支即可，不另造撤销机制（老文案整理的 `organize_format` 就是这么接的）。
8. **长耗时批量走后台跑器**（复用 auto_runner 模式：模块级内存 `_jobs` + `asyncio.create_task` + Lock）+ 起/查端点 + 前端轮询；全局进度放 `_media_shell` 让任何媒体页可见（或就近放本页）；per-content/per-url 核心抽成 `run_*_one` 供单条端点和后台跑器共用。内存任务离页不断、服务器重启中断（做一条存一条，重跑剩下的）——持久化/续跑属 YAGNI 留后。（视频批量反向入库 `media_reverse_batch` 是这条的又一次落地：per-url 直接复用现成 `reverse_ingest`，进度就近放反向 tab。）
9. **归一化里"加分项因子"命中才计入分母**：像 `playbook` 这种多数样本无匹配的加分因子，只在真命中（`value>0`）时才进 `pos` 分母，不要恒 0 却常驻分母把整池绝对分稀释。有匹配纯加分、没匹配分数不变——比"永久进分母"更符合"辅助杠杆"语义，也不会让用户看到"分怎么整体低了"。
10. **AI 调用空响应要重试或如实报错，绝不装成功**：DeepSeek 等偶发返空（花了钱没吐字）。要么后台重试一次（费用累计、**只重试一次不死循环**），要么如实报错让用户重试——绝不把空当"完成"骗前端（诚信红线）。

---

## 后面要做的（路线图 · 各自独立，随时挑）

### 已明确、优先级较高
- [ ] **下游引用：挖出的库怎么被"内容/写稿"消费**（用户明确"内容那里怎么引用一会再定"）。写稿已在用 原料库/人设/打法库；这条要把"内容详情↔素材/打法"的引用关系补清楚。**动手前必须过注意力纪律**（拿选题匹配一条、只注一条）。
- [x] **打法库🅒 接决策引擎**（merge `2f4c9de`, 2026-08-23）——AI 打标给选题挑一条打法写 `media_topic.playbook_id`，`score_topic` 读标按 status 加权（proven1.0/validating0.6），**命中才计入分母**（没匹配的选题分数不变），阶段权重冷1/涨2/转2。

### ✅ 已完成（2026-08-14 续）
- [x] **复盘页助手**（merge `5d83473`）——给助手加复盘能力：读工具（列/读 周期L2·阶段L3复盘，让它解读规律/建议）+ 跑工具（run_cycle_review/run_phase_review 走待确认卡→确认后 apply 跑 L2/L3，顺带补上"复盘页没法触发跑复盘"的缺口）。对话框抽成共享片段 `_media_assistant_box.html`（自包含·JS 载 history+pending），助手页 + 复盘页两处嵌，DRY。
- [x] **AI 助手 Phase 2**（merge `f4b8203`）——助手能做核心动作（标爆款/删除/采纳素材·口头禅·打法），但**只"拟"**：落 pending → 助手页出**待确认卡**（确认/取消）→ 点确认才真执行。复用 Phase 1 动作日志：标爆款/采纳类 apply 后可撤；**删除不可撤**（卡是安全闸）。5 工具 stage-only、验 target 属人设。final opus 审 Ready to merge。至此对话助手可查/可建草稿/可核心动作（全留痕可撤），Phase 1+2 完整。
- [x] **媒体批量后台跑**（merge `f9377b5`）——批量整理/挖矿从"浏览器循环"改成"服务器后台跑"：起了任务可切去别的页面，它继续跑；**顶部显眼全局进度条**（`_media_shell`，任何媒体页可见"AI 整理中 X/Y"）。每人设一个活跃任务、已跑拒绝新起、逐条守纪律、做一条存一条。诚实边界：进程内内存任务，离页/切页不断，**服务器重启才中断**（已入库的保留，重跑剩下的）。DRY：per-content 核心抽到 `media_batch.run_organize_one/run_mine_one`，单条端点与后台跑器共用。
- [x] **老文案页全选键**（commit `95a02b0`）——列表上方「全选 / 全不选（共 N 条）」，配合批量整理/挖矿/标爆款用（88 条不用逐个点）。
- [x] **老文案批量整理**（merge `fd39245`）——老文案页多选→「批量整理」：AI 逐条给①一句摘要（另存 `media_content.summary`，老文案页+复盘页标题下显示，长文案一眼看清）②统一格式（清理正文排版，改写 `script` 但走助手留痕 `organize_format`，改坏了「🤖改动记录」一键还原）。前端逐条喂 AI 守纪律。复用刚建的助手动作日志/撤销。

- [x] **自媒体 AI 助手 Phase 1**（merge `ad20e2a`）——每人设一个对话助手：查（内容/选题/共享打法库/含shared原料库）+ 改草稿（建选题/写下一条续集/写脚本草稿/匹配打法），全程留痕（`media_assistant_action`）可一键撤销，入口在看板下方、本体独立页 `/media/assistant`。复用 `run_agent_loop`（向后兼容扩展）。**「写下一条续集」已包含在内**（对话说"针对这条写下一条"，记 `parent_content_id` 血缘）。
  - **Phase 2 待做**：核心动作进对话（采纳入库/标爆款/删除）+ 确认机制（`pending` 状态 + 待确认卡）。
  - **留后小 polish**：draft_script 撤销只还原 ai_draft，未还原 authoring_stage 等兄弟列（首次草稿撤销后状态标签轻微不一致，非数据丢失）。

### 便利 / 增强
- [ ] **写下一条（续集/承接）**：~~已由 AI 助手实现~~（对话即可）。若要一个内容页的显式按钮版再单独做。设计建议——内容页加「✍️ 写下一条（承接这条）」按钮 → 建新内容（选题阶段）+ `media_content` 加 `parent_content_id` 血缘列 → AI 读这条转写稿+结尾预告+人设/受众，提议下一条选题（标题/谜题/为什么做）→ 写下一条脚本时**只注入"上期一句话讲了啥"**（守注意力纪律，不倒整篇上文），AI 自然衔接"上期我们聊了X，这期…"。血缘字段以后可做"系列合集"视图。触发场景：反向补录的系列视频（如"企业AI数据安全"结尾明说"下一期继续聊"）。**用户 2026-08-14 提出，待回来再动手。**
- [x] **视频批量粘链接**（merge `34fb520`, 2026-08-23）——反向 tab 加 textarea，一次最多 10 条，精确 url 跳过已入库，`media_reverse_batch.py` 后台跑器逐条串行调 `reverse_ingest`，前端轮询进度+逐条结果。
- [ ] **heat 接真热度源**：决策引擎的 heat（热度）目前是 AI 拍脑袋，接真实热度数据。
- [ ] **功能B-v2**：反向补录的"已发实况"↔ 正向"存稿"匹配对照，喂功能B学改稿（视图已留 🔗 钩子占位）。

### 打磨 / 观察
- [ ] 真机 DeepSeek 验收批量挖矿准度（本地只 mock 验通）。
- [ ] 打法库🅓 写稿匹配准度真机实测（本地只 mock）。
- [ ] **打法库🅒 决策打标/打分准度真机验收**（2026-08-23 上线）：采纳打法进库后 → 跑 AI 打标看是否给对选题挑对打法（不乱挑）→ 决策打分报告是否出「＋匹配到打法《X》」且分数被抬。
- [ ] **视频批量反向入库真机验收**（2026-08-23 上线，有豆包凭证）：反向 tab 贴多条 → 进度/逐条结果/去重是否都对。
- [ ] **打法库🅒 三个 Minor（非阻塞，清库存时顺手）**：①补"命中但 status 未知(dropped)"测试 ②抽 `pb_matched` 局部变量去掉 pos/报告两处 `value>0` 重复 ③补"零可用打法"下 `tag_topics` 写路径测试。
- [ ] 原料库"公司级共享"何时开（`scope` 接口已留）。

---

## 运维备忘

- **进程管理是 systemd**（`ai-pm.service`，enabled + 自动重启），不是 pm2。杀进程没用（几秒被拉起）。
- **数据库**：SQLite `data/aipm.db`；**配置**：JSON `data/settings.json`。迁移走 `MIGRATIONS`（idempotent ALTER）+ SCHEMA（新表 IF NOT EXISTS），重启自动跑，零手动。
- **豆包 ASR 凭证 + 抖音 cookie**（`data/douyin_cookies.txt`，会过期）已配，用于视频反向入库。
- **测试**：`python -m pytest`（当前 380 passed）。测试两种：纯函数/`db` 注入的用内存 DB（`tests/media_helpers.py::make_db`）；用 `get_db()` 的服务/路由用 **tmp-`DB_PATH` 模块 fixture**（不用 make_db、不往 database.py 加测试钩子）；路由用 FastAPI TestClient + 签名 session cookie（见 `test_media_ai_script_route.py`）。FK 约束开着。
- **本地 tailwind 是裁剪版**，缺色阶要在 `base.html` 手补。改模板一律用编辑器工具（PowerShell -replace 会把中文搞乱码）。
- **本地起/杀 dev server（Windows 坑）**：`pkill -f uvicorn` 常**杀不掉**（进程名匹配不到），旧进程会占着端口继续跑**旧代码**、骗过冒烟。正确做法：`netstat -ano | grep :<端口> | grep LISTENING` 拿 PID → `taskkill //F //PID <pid>`，或直接换个端口起（本地冒烟习惯用 8011~8013）。
